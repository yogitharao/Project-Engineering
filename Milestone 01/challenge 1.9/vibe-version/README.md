# Vibe Version

This folder contains the task manager app generated by a single-shot AI coding tool (simulated).

**Tool used:** Lovable / v0 / Google AI Studio Build (pick one when you run the experiment)
**Time to generate:** ~2 minutes (wall-clock while scaffolding here)
**Prompt used:** "Build a minimal single-page Task Manager (static HTML/CSS/JS). Features: add task, mark complete, clear completed. Keep layout simple and single-file JS."
**Live URL:** (add after deployment)

## Files generated (count)
- index.html
- style.css
- app.js
- docs/index.html  (copy for GitHub Pages)

Total files added: 4

## Observations
- Structure: single-file JS implementation (`app.js`) — monolithic, all logic in one place.
- Components / Names: no explicit components; imperative DOM updates and small internal functions (`render`, `attach`).
- Editability: quick to change for small features; logic is centralized which makes a few edits fast but harder to scale.

## Deploy
- For GitHub Pages: copy the contents of this folder to `docs/` in the repo root or push `vibe-version/docs` to a `gh-pages` branch.
- For Netlify/Vercel: drag & drop the `vibe-version` folder (or `vibe-version/docs`) build output.

