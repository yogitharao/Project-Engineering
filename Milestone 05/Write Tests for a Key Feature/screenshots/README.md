# Submission screenshots

Replace placeholders with **real terminal captures**:

1. **`tests-passing.png`** — Run `npm test` and screenshot the summary (`14 passed`, 4 suites, 0 failures).
2. **`test-coverage.png`** — Run `npm run test:coverage` and screenshot the coverage table.

Reference output is also in `test-output.txt` (last lines of a passing run).
